name = "vertex-experience"
compatibility_date = "2024-12-14"
pages_build_output_dir = "."

# This is a static site - no Workers configuration needed
# Headers are configured via _headers file for Pages

# Note: For Cloudflare Pages, build configuration is done in the dashboard
# This file is kept minimal for Pages deployment
